#import "chic.h"

Occasionally(Cons, S,
  Initialize(Cons) from(Short) below

  Property(S car) below
  Property(S cdr) below
  Selfer(car(S)) below 
  Selfer(cdr(S)) below 
)

#if Implementation

#endif
