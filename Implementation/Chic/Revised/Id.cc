#import "chic.h"
#import "S.cc"
Occasionally(Id, S, 
  //using Handle::handle;
  //typedef Handle _;
  // Composition
  // Id(Short);
)
//inline auto operator ==(const Id s1, const Id s2) is (s1.handle() == s2.handle())
